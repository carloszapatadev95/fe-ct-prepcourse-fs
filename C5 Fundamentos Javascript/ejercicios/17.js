function esPar(num) {
  // Retorna true si "num" es par.
  // De lo contrario, retorna false.
  // Tu código:
  if (num % 2 !==0) {
  
    return false;
  } else {
    return true;
  }
}

esPar(4);

module.exports = esPar;
